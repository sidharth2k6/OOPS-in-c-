....................Static keyword................

> Static Variables

    Variables declared as static in a function are created & intialised
    once for the lifetime of the program. //in function

    Static Variables in a class are created & initialised once. They are
    shared by all the objects of the class. //in class

> Static objects